def add_dishes()->None:
    #function will add dishes to the person whose face is recognized by the machine.

def remove_dishes()->None:
    #function will remove all the dishes of the person it detects
def calculate_max()->:
    #max dishes at end of day
def person_detected():
    #tells camera to take a pic when person is detected
    #add dishes here too depending on if we're using the IR to detect the person or not
    #if not using IR, then this function increments the person's dish count every time


def main():
    #call counter from display.py
    



if __name__=='__main__':
    main()

